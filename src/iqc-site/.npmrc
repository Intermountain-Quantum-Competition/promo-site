# Without this, the "engines" field in package.json is advisory: npm prints a warning and
# installs anyway, and a too-old Node then fails later with an unrelated-looking error.
# engine-strict makes the install fail immediately, with the required range.
engine-strict=true
