dist
dist-ssr
coverage
node_modules
public
src/assets

# npm rewrites these on every install; let it own their formatting rather than
# fighting the pre-commit hook over it.
package.json
package-lock.json
